import React, {Component} from 'react';
import {Image} from 'react-bootstrap';

export default class Current extends Component{
    render(){
        return(
            <div className="Current">
                <Image className="Current_icon" src="" responsive/>
            </div>
        );
    }
}